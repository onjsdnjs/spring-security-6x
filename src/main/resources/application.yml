spring:
  security:
    oauth2:
      resourceserver:
        jwt:
          jwk-set-uri: http://localhost:9000/oauth2/jwks

  datasource:
    url: jdbc:h2:mem:testdb
    driver-class-name: org.h2.Driver
    username: sa
    password:

  h2:
    console:
      enabled: true
      path: /h2-console
  auth:
    context-mode: JWT
    enabled-auth-types:
      - FORM
      - OTT
    token-control-mode: JWTS

  mail:
    host: smtp.gmail.com        # SMTP 호스트
    port: 587                   # TLS 포트
    username: onjsdnjs@naver.com   # 발신용 계정
    password: leaven011806231!      # 앱 비밀번호 또는 OAuth 토큰
    properties:
      mail.smtp.auth: true
      mail.smtp.starttls.enable: true



